<div>
    <nav class="navbar navbar-default navbar-fixed-top" role="navigation">
        <div class="navbar-header habmin-nav">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <span class="navbar-brand">{{t mainMenuAppName}}</span>
        </div>
        <div class="collapse navbar-collapse navbar-ex1-collapse">
            <ul class="nav navbar-nav">
                <li><a href="#"><span class="glyphicon glyphicon-tasks"></span> {{t mainMenuDashboard}}
                </a></li>
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown"><span
                            class="glyphicon glyphicon-eye-open"></span> {{t mainMenuSitemaps}} <span
                            class="caret"></span></a>
                    <ul class="dropdown-menu" role="menu">
                        {{#each item in controller}}
                            <li>{{#link-to 'sitemap.view' item.name}}{{item.label}}{{/link-to}}</li>
                        {{/each}}
                    </ul>
                </li>
                <li>{{#link-to 'chart'}} {{t mainMenuAutomation}}{{/link-to}}</li>
            </ul>
            <form class="navbar-form navbar-left" role="search">
                <div class="form-group">
                    <input type="text" class="form-control" placeholder="Search">
                </div>
                <button type="submit" class="btn btn-default">Submit</button>
            </form>
            <ul class="nav navbar-nav navbar-right">
                <li><a href="#">Link</a></li>
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">Dropdown <span class="caret"></span></a>
                    <ul class="dropdown-menu" role="menu">
                        {{#each item in controller}}
                            <li>{{#link-to 'sitemap.view' item.name}}{{item.label}}{{/link-to}}</li>
                        {{/each}}
                    </ul>
                </li>
            </ul>
        </div>
    </nav>
    <div class="container" id="main">
        <div class="row">
            {{outlet}}
        </div>
    </div>
</div>
