<!-- Binding configuration for the Epson Projector binding in openHAB -->
<binding>
	<!-- Full name presented to the user -->
	<name>Epson Projector Binding</name>
	<!-- PID name used within the OSGi layer -->
	<pid>epsonprojector</pid>
	<!-- Binding type: protocol, ui -->
	<type>device</type>
	<!-- Author -->
	<author>Pauli Anttila</author>
	<!-- Current version of the binding to which the file is applicable -->
	<version>1.3.0</version>
	<!-- Minimum version of openHAB required to run this binding -->
	<oh_version>1.3.0</oh_version>

    <!-- Binding configuration settings -->
    <binding.config>
    </binding.config>

	<!-- The following lines specify the communications interface settings. These may be repeated multiple times -->
    <binding.interface>
        <interface.setting>
            <name>serialPort</name>
            <label>Serial Port</label>
            <description>Set the Serial Port of the interface.</description>
            <optional>false</optional>
            <defaultvalue></defaultvalue>
            <minimum></minimum>
            <maximum></maximum>
            <values></values>
        </interface.setting>
    </binding.interface>
    
    <!-- Item binding configuration -->
    <binding.items>
        <binding.io>
        </binding.io>
    </binding.items>
</binding>
